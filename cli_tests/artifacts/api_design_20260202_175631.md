# API 설계서

**버전**: 1.0.0
**작성일**: 2026년 02월 02일
**작성자**: CAAS

---

## 1. API 개요

### 1.1 목적
본 문서는 시스템의 REST API 설계를 정의합니다.

### 1.2 기본 정보

**Base URL**: `http://localhost:8000`

**API 버전**: v1

**프로토콜**: HTTP/HTTPS

**인증**: API Key / Bearer Token

---

## 2. API 엔드포인트


### 2.1 사용자가 새로운 작업을 생성합니다. 제목, 설명 및 마감일을 포함합니다.

**엔드포인트**: `POST /api/v1/create_task`

**설명**: 사용자가 새로운 작업을 생성합니다. 제목, 설명 및 마감일을 포함합니다.

**Request**:
```json
{
  "input": "string",
  "options": {}
}
```

**Response**:
```json
{
  "status": "success",
  "data": 생성된 작업의 고유 task_id와 생성 타임스탬프,
  "message": "string"
}
```

**Status Codes**:
- `200 OK`: 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 실패
- `500 Internal Server Error`: 서버 오류

---


### 2.2 특정 작업의 고유 식별자를 사용하여 세부 정보를 조회합니다.

**엔드포인트**: `POST /api/v1/read_task`

**설명**: 특정 작업의 고유 식별자를 사용하여 세부 정보를 조회합니다.

**Request**:
```json
{
  "input": "string",
  "options": {}
}
```

**Response**:
```json
{
  "status": "success",
  "data": 조회된 작업의 세부 정보,
  "message": "string"
}
```

**Status Codes**:
- `200 OK`: 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 실패
- `500 Internal Server Error`: 서버 오류

---


### 2.3 기존 작업의 세부 정보를 업데이트합니다. 제목, 설명 및 마감일을 포함합니다.

**엔드포인트**: `POST /api/v1/update_task`

**설명**: 기존 작업의 세부 정보를 업데이트합니다. 제목, 설명 및 마감일을 포함합니다.

**Request**:
```json
{
  "input": "string",
  "options": {}
}
```

**Response**:
```json
{
  "status": "success",
  "data": 업데이트된 작업의 세부 정보,
  "message": "string"
}
```

**Status Codes**:
- `200 OK`: 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 실패
- `500 Internal Server Error`: 서버 오류

---


### 2.4 특정 작업을 고유 식별자를 사용하여 삭제합니다.

**엔드포인트**: `POST /api/v1/delete_task`

**설명**: 특정 작업을 고유 식별자를 사용하여 삭제합니다.

**Request**:
```json
{
  "input": "string",
  "options": {}
}
```

**Response**:
```json
{
  "status": "success",
  "data": 작업 삭제 성공 메시지,
  "message": "string"
}
```

**Status Codes**:
- `200 OK`: 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 실패
- `500 Internal Server Error`: 서버 오류

---


### 2.5 사용자가 이메일과 비밀번호로 로그인합니다.

**엔드포인트**: `POST /api/v1/user_login`

**설명**: 사용자가 이메일과 비밀번호로 로그인합니다.

**Request**:
```json
{
  "input": "string",
  "options": {}
}
```

**Response**:
```json
{
  "status": "success",
  "data": 로그인 성공 또는 실패 메시지,
  "message": "string"
}
```

**Status Codes**:
- `200 OK`: 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 실패
- `500 Internal Server Error`: 서버 오류

---


## 3. 공통 요소

### 3.1 인증

**Header**:
```
Authorization: Bearer {token}
```

### 3.2 응답 형식

```json
{
  "status": "success|error",
  "data": {},
  "message": "string",
  "timestamp": "ISO 8601"
}
```

### 3.3 에러 응답

```json
{
  "status": "error",
  "error": {
    "code": "ERROR_CODE",
    "message": "Human readable message"
  }
}
```

---

**문서 이력**

| 버전 | 일자 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| 1.0.0 | 2026-02-02 | 초안 작성 | CAAS |

---

*본 문서는 CAAS (CrewAI Agent Auto-generation System)에 의해 자동 생성되었습니다.*