# 작업 관리 대시보드

Auto-generated CrewAI multi-agent system.

## Features

- User Login: Allow users to log in with email and password.\n- User Registration: Allow new users to create an account using email and password.\n- User Logout: Allow users to log out from their account.\n- Two-Factor Authentication: Provide an additional layer of security by requiring a second form of verification during login.\n- Create Task: Allow users to create a new task with details such as title, description, due date, and priority level.

## Setup

1. Install dependencies:
```bash
pip install -r requirements.txt
```

2. Set up environment variables:
```bash
cp .env.example .env
# Edit .env and add your API keys
```

3. Run the application:
```bash
python main.py
```

## Project Structure

- `main.py` - Main execution script
- `agents.py` - Agent definitions
- `tasks.py` - Task definitions
- `requirements.txt` - Python dependencies
- `.env.example` - Environment variables template

## Generated by CAAS Framework

This project was automatically generated using the CrewAI Agent Auto-generation System (CAAS).
