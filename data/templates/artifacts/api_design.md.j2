# {{ metadata.title }}

**버전**: {{ metadata.version }}
**작성일**: {{ metadata.created_at | datetime("%Y년 %m월 %d일") }}
**작성자**: {{ metadata.created_by }}

---

## 1. API 개요

### 1.1 목적
본 문서는 시스템의 REST API 설계를 정의합니다.

### 1.2 기본 정보

**Base URL**: `http://localhost:8000`

**API 버전**: v1

**프로토콜**: HTTP/HTTPS

**인증**: API Key / Bearer Token

---

## 2. API 엔드포인트

{% if tasks %}
{% for task in tasks %}

### 2.{{ loop.index }} {{ task.description[:50] if task.description else task.name }}

**엔드포인트**: `POST /api/v1/{{ task.id or task.name | lower | replace(' ', '-') }}`

**설명**: {{ task.description or "API 엔드포인트" }}

**Request**:
```json
{
  "input": "string",
  "options": {}
}
```

**Response**:
```json
{
  "status": "success",
  "data": {{ task.expected_output or "object" }},
  "message": "string"
}
```

**Status Codes**:
- `200 OK`: 성공
- `400 Bad Request`: 잘못된 요청
- `401 Unauthorized`: 인증 실패
- `500 Internal Server Error`: 서버 오류

---

{% endfor %}
{% endif %}

## 3. 공통 요소

### 3.1 인증

**Header**:
```
Authorization: Bearer {token}
```

### 3.2 응답 형식

```json
{
  "status": "success|error",
  "data": {},
  "message": "string",
  "timestamp": "ISO 8601"
}
```

### 3.3 에러 응답

```json
{
  "status": "error",
  "error": {
    "code": "ERROR_CODE",
    "message": "Human readable message"
  }
}
```

---

**문서 이력**

| 버전 | 일자 | 변경 내용 | 작성자 |
|------|------|-----------|--------|
| {{ metadata.version }} | {{ metadata.created_at | datetime("%Y-%m-%d") }} | 초안 작성 | {{ metadata.created_by }} |

---

*본 문서는 CAAS (CrewAI Agent Auto-generation System)에 의해 자동 생성되었습니다.*
