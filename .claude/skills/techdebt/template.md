# Technical Debt Report - {{ date }}

## Executive Summary
- **Total issues**: {{ total_issues }}
- **High priority**: {{ high_priority }}
- **Medium priority**: {{ medium_priority }}
- **Low priority**: {{ low_priority }}
- **Estimated effort**: {{ total_effort }}

## 🔴 Critical Issues (Fix Immediately)

{% for issue in critical_issues %}
### {{ issue.category }}: {{ issue.title }}
- **Location**: `{{ issue.file }}:{{ issue.line }}`
- **Type**: {{ issue.type }}
- **Impact**: {{ issue.impact }}
- **Effort**: {{ issue.effort }}
- **Fix**: {{ issue.fix }}

{% endfor %}

## 🟡 Medium Priority (Next Sprint)

{% for issue in medium_issues %}
### {{ issue.category }}: {{ issue.title }}
- **Location**: `{{ issue.file }}:{{ issue.line }}`
- **Type**: {{ issue.type }}
- **Impact**: {{ issue.impact }}
- **Effort**: {{ issue.effort }}
- **Fix**: {{ issue.fix }}

{% endfor %}

## 🟢 Low Priority (Backlog)

{% for issue in low_issues %}
### {{ issue.category }}: {{ issue.title }}
- **Location**: `{{ issue.file }}:{{ issue.line }}`
- **Type**: {{ issue.type }}
- **Impact**: {{ issue.impact }}
- **Effort**: {{ issue.effort }}

{% endfor %}

## Automated Fixes Available

- [ ] Remove unused imports: `autoflake --in-place --remove-all-unused-imports -r caas_framework/ caas_cli/ caas_sdk/`
- [ ] Format code: `black caas_framework/ caas_cli/ caas_sdk/`
- [ ] Sort imports: `isort caas_framework/ caas_cli/ caas_sdk/`
{% for fix in auto_fixes %}
- [ ] {{ fix.description }}: `{{ fix.command }}`
{% endfor %}

## Refactoring Recommendations

{% for refactor in refactorings %}
### {{ loop.index }}. {{ refactor.name }}

**Problem**: {{ refactor.problem }}

**Locations**:
{% for loc in refactor.locations %}
- `{{ loc.file }}:{{ loc.line }}`
{% endfor %}

**Proposed solution**: {{ refactor.solution }}

**Effort**: {{ refactor.effort }}

**Benefits**: {{ refactor.benefits }}

{% endfor %}

## Code Metrics

| Metric | Before | After (Projected) | Improvement |
|--------|--------|-------------------|-------------|
| Total LOC | {{ metrics.loc_before }} | {{ metrics.loc_after }} | -{{ metrics.loc_reduction }}% |
| Duplicate blocks | {{ metrics.duplicates_before }} | {{ metrics.duplicates_after }} | -{{ metrics.duplicates_reduction }}% |
| Unused imports | {{ metrics.unused_imports }} | 0 | -100% |
| Test coverage | {{ metrics.coverage_before }}% | {{ metrics.coverage_after }}% | +{{ metrics.coverage_improvement }}% |

## Next Steps

1. **Immediate** (This sprint):
   {% for step in immediate_steps %}
   - {{ step }}
   {% endfor %}

2. **Short-term** (Next 2 sprints):
   {% for step in short_term_steps %}
   - {{ step }}
   {% endfor %}

3. **Long-term** (Backlog):
   {% for step in long_term_steps %}
   - {{ step }}
   {% endfor %}

---

**Generated by**: `/techdebt` skill
**Report date**: {{ date }}
**Analyzed paths**: {{ analyzed_paths }}
