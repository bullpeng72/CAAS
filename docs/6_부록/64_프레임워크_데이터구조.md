# 64. CAAS 프레임워크 데이터 구조

**Version**: v0.6.4
**Last Updated**: 2026-03-13
**대상 독자**: 프레임워크 기여자, 고급 사용자, 통합 개발자

---

## 개요

CAAS 프레임워크는 자연어 요구사항을 프로덕션 코드로 변환하는 과정에서 세 가지 핵심 지식 체계를 활용합니다.

1. **온톨로지 시스템** — 도구·에이전트 역할·도메인 개념 정의
2. **도메인 분류 시스템** — 요구사항의 성격을 분류하고 적절한 코드 생성 전략 선택
3. **템플릿 시스템** — Jinja2 기반 프로덕션 코드 생성
4. **Golden Data 모델** — Phase 0의 핵심 구조화 데이터

이 문서는 각 시스템의 내부 구조, 데이터 모델, 상호 관계를 상세히 설명합니다.

---

## 1. 전체 데이터 흐름

```
자연어 요구사항
    │
    ▼ Phase 0: Concretization
┌─────────────────────────────────────┐
│  ConcretizedRequirement (Golden Data)│
│  - FeatureSpec[]                    │
│  - DataModel[]                      │
│  - UIComponent[]                    │
│  - BoundariesSpec                   │
│  - CommandsSpec                     │
│  - CodeStyleSpec / GitWorkflowSpec  │
└─────────────────────────────────────┘
    │
    ├──▶ Domain Ontology (DomainType 분류)
    │         │
    │         ▼
    │    DomainStrategyConfig
    │    (AGENT_BASED / CRUD_BASED / HYBRID)
    │
    ├──▶ Tool Ontology (도구 추천)
    │         │
    │         ▼
    │    ConceptualTool[] → 구체적 구현체
    │
    └──▶ Agent Role Ontology (역할 매핑)
              │
              ▼
         AgentRole × TaskType → ROLE_TASK_MAPPINGS
              │
              ▼
         Jinja2 템플릿 렌더링 (26개 .j2)
              │
              ▼
         프로덕션 코드
         (agents.py, tasks.py, crew.py, main.py, tools.py, tests/, ...)
```

---

## 2. Golden Data 모델 (ConcretizedRequirement)

**위치**: `caas_framework/methodology/golden_data.py`, `caas_framework/models/specifications.py`

Phase 0 (Concretization)의 출력이자 이후 모든 Phase의 입력 데이터입니다.

### 2.1 최상위 구조

```python
class ConcretizedRequirement(BaseModel):
    project_name: str                    # 프로젝트명
    domain: DomainType                   # 분류된 도메인
    description: str                     # 정제된 요구사항 설명
    features: List[FeatureSpec]          # 기능 명세 목록
    data_models: List[DataModel]         # 데이터 모델 목록
    ui_components: List[UIComponent]     # UI 컴포넌트 (명시적 UI 요청 시만 생성)
    boundaries: BoundariesSpec           # 성능/보안/확장성 제약
    commands: CommandsSpec               # CLI 명령어 정의
    code_style: CodeStyleSpec            # 코딩 컨벤션
    git_workflow: GitWorkflowSpec        # Git 브랜치/커밋 전략
```

### 2.2 FeatureSpec — 기능 명세

```python
class FeatureSpec(BaseModel):
    id: str                              # 기능 고유 ID (예: "F001")
    name: str                            # 기능명 (한국어)
    description: str                     # 상세 설명
    priority: str                        # "HIGH" | "MEDIUM" | "LOW"
    inputs: List[str]                    # 입력 파라미터 목록
    outputs: List[str]                   # 출력 데이터 목록
    dependencies: List[str]              # 의존 기능 ID 목록
    acceptance_criteria: List[str]       # 완료 기준 (TDD 테스트 생성에 활용)
    tools_needed: List[str]              # 필요 도구 힌트
```

**활용**:
- Phase 1 (Discovery): 도메인 분류 기준
- Phase 3 (Design): Agent/Task 개수 결정
- Phase 5 (Delivery): TDD 테스트 케이스 자동 생성

### 2.3 DataModel — 데이터 모델

```python
class DataModel(BaseModel):
    name: str                            # 모델명 (예: "Task", "User")
    fields: List[FieldSpec]              # 필드 목록
    relationships: List[str]             # 관계 설명 (예: "User has many Tasks")
    storage: str                         # "sqlite" | "postgresql" | "memory" | "file"

class FieldSpec(BaseModel):
    name: str
    type: str                            # Python 타입 힌트
    required: bool
    description: str
```

**활용**:
- `backend/models.j2` 템플릿 → Pydantic / SQLAlchemy 모델 생성
- `backend/api.j2` 템플릿 → FastAPI CRUD 엔드포인트 생성

### 2.4 UIComponent — UI 컴포넌트

```python
class UIComponent(BaseModel):
    component_type: str                  # "form" | "dashboard" | "table" | "chart"
    name: str
    fields: List[str]                    # 폼 필드 목록
    actions: List[str]                   # 버튼/액션 목록
    framework: str                       # "streamlit" | "react" | "fastapi"
```

> **주의**: `ui_components`는 요구사항에 명시적인 UI 키워드(`streamlit`, `대시보드`, `웹 UI` 등)가 있을 때만 생성됩니다. 기본값은 빈 리스트(`[]`)입니다. (v0.6.4 UI 자동감지 버그 수정)

### 2.5 BoundariesSpec — 비기능 요구사항

```python
class BoundariesSpec(BaseModel):
    performance: Dict[str, Any]
    # 예: {"response_time_ms": 200, "throughput_rps": 100, "concurrent_users": 50}

    security: Dict[str, Any]
    # 예: {"authentication": "JWT", "authorization": "RBAC", "data_encryption": true}

    scalability: Dict[str, Any]
    # 예: {"horizontal_scaling": true, "caching": "Redis", "database_sharding": false}

    reliability: Dict[str, Any]
    # 예: {"uptime_sla": "99.9%", "backup_frequency": "daily"}
```

**활용**: QA 시스템 (`caas_framework/qa/performance_tester.py`)의 성능 테스트 기준으로 사용됩니다.

### 2.6 CommandsSpec — CLI 명령어 정의

```python
class CommandsSpec(BaseModel):
    main_command: str                    # 기본 실행 명령어 (예: "python main.py")
    subcommands: List[CommandDef]        # 서브커맨드 목록
    environment_variables: List[str]     # 필요한 환경변수 목록

class CommandDef(BaseModel):
    name: str
    description: str
    arguments: List[ArgDef]
    options: List[OptionDef]
```

### 2.7 CodeStyleSpec / GitWorkflowSpec

```python
class CodeStyleSpec(BaseModel):
    language: str                        # "python"
    formatter: str                       # "black"
    linter: str                          # "flake8" | "ruff"
    type_checker: str                    # "mypy"
    max_line_length: int                 # 100
    docstring_style: str                 # "google" | "numpy"

class GitWorkflowSpec(BaseModel):
    main_branch: str                     # "main" | "master"
    feature_branch_pattern: str          # "feature/{description}"
    commit_message_format: str           # Conventional Commits
    release_strategy: str                # "gitflow" | "github-flow"
```

---

## 3. 도메인 분류 시스템

**위치**: `caas_framework/models/domain_types.py`, `caas_framework/codegen/domain_strategy.py`

### 3.1 DomainType 열거형 (17개)

```python
class DomainType(str, Enum):
    # 대화 & 커뮤니케이션
    CONVERSATIONAL_AI = "CONVERSATIONAL_AI"
    CUSTOMER_SUPPORT = "CUSTOMER_SUPPORT"

    # 작업 & 워크플로우
    TASK_MANAGEMENT = "TASK_MANAGEMENT"
    WORKFLOW_AUTOMATION = "WORKFLOW_AUTOMATION"

    # 데이터 & 분석
    DATA_ANALYSIS = "DATA_ANALYSIS"
    REPORT_GENERATION = "REPORT_GENERATION"
    DASHBOARD = "DASHBOARD"

    # 콘텐츠 & 문서
    CONTENT_CREATION = "CONTENT_CREATION"
    DOCUMENT_PROCESSING = "DOCUMENT_PROCESSING"
    KNOWLEDGE_BASE = "KNOWLEDGE_BASE"

    # 통합 & API
    API_INTEGRATION = "API_INTEGRATION"
    WEBHOOK_HANDLER = "WEBHOOK_HANDLER"

    # 도메인 특화
    E_COMMERCE = "E_COMMERCE"
    EDUCATION = "EDUCATION"
    HEALTHCARE = "HEALTHCARE"
    FINANCE = "FINANCE"
    CUSTOM = "CUSTOM"
```

### 3.2 도메인 키워드 매핑

**위치**: `data/ontology/domain_keywords.json` (226개 키워드)

각 도메인별 한국어/영어 키워드 목록으로, 자연어 요구사항에서 도메인을 자동 분류합니다.

```json
{
  "DATA_ANALYSIS": {
    "keywords": ["분석", "통계", "시각화", "차트", "그래프", "인사이트",
                 "데이터 처리", "ETL", "머신러닝", "ML", "예측", "모델링"],
    "weight": 1.0
  },
  "CONTENT_CREATION": {
    "keywords": ["블로그", "기사", "SEO", "포스팅", "콘텐츠 캘린더",
                 "소셜미디어", "마케팅", "카피라이팅"],
    "weight": 1.0
  }
}
```

### 3.3 CodeGenStrategy — 코드 생성 전략

```python
class CodeGenStrategy(str, Enum):
    AGENT_BASED = "AGENT_BASED"   # CrewAI 멀티 에이전트 중심
    CRUD_BASED = "CRUD_BASED"     # DB + REST API 중심
    HYBRID = "HYBRID"             # 에이전트 + CRUD 혼합
```

### 3.4 도메인 → 전략 매핑표

| DomainType | CodeGenStrategy | 주요 생성 파일 | 설명 |
|-----------|----------------|------------|------|
| CONVERSATIONAL_AI | AGENT_BASED | agents.py, tasks.py, crew.py | CrewAI 에이전트 중심 |
| CUSTOMER_SUPPORT | AGENT_BASED | + tools.py (FAQ tool) | 도구 사용 에이전트 |
| DATA_ANALYSIS | AGENT_BASED | + reports/, visualizations/ | 분석 파이프라인 |
| CONTENT_CREATION | AGENT_BASED | + content/, templates/ | 콘텐츠 생성 파이프라인 |
| REPORT_GENERATION | AGENT_BASED | + reports/, charts/ | 보고서 생성 |
| TASK_MANAGEMENT | CRUD_BASED | models.py, api.py, database.py | DB 중심 CRUD |
| API_INTEGRATION | CRUD_BASED | api_client.py, webhooks.py | REST API 클라이언트 |
| WEBHOOK_HANDLER | CRUD_BASED | handlers.py, events.py | 이벤트 처리 |
| WORKFLOW_AUTOMATION | HYBRID | agents.py + scheduler.py | 에이전트 + 일정 |
| DASHBOARD | HYBRID | agents.py + app.py (Streamlit) | 분석 + UI |
| KNOWLEDGE_BASE | HYBRID | agents.py + vector_db/ | 검색 + 에이전트 |
| E_COMMERCE | HYBRID | models.py + agents.py | 상품+주문 CRUD + 추천 |
| EDUCATION / HEALTHCARE / FINANCE | HYBRID | 도메인별 특화 | 규정 준수 포함 |
| CUSTOM | AGENT_BASED (기본) | — | 범용 처리 |

### 3.5 DomainStrategyConfig 구조

```python
@dataclass
class DomainStrategyConfig:
    domain: DomainType
    strategy: CodeGenStrategy
    primary_patterns: List[str]     # 주요 코드 패턴
    recommended_tools: List[str]    # 추천 도구 목록
    agent_roles: List[str]          # 권장 에이전트 역할
    min_agents: int                 # 최소 에이전트 수
    max_agents: int                 # 최대 에이전트 수
    enable_crud: bool               # CRUD 레이어 포함 여부
    enable_api: bool                # REST API 생성 여부
    enable_frontend: bool           # 프론트엔드 생성 여부 (기본값 False)
    special_requirements: Dict      # 도메인별 특수 요구사항
```

---

## 4. 온톨로지 시스템

### 4.1 도구 온톨로지

**위치**: `data/ontology/tools.json`, `caas_framework/knowledge/ontology/tool_ontology.py`

#### ToolType 분류 (8개)

```python
class ToolType(str, Enum):
    WEB_SEARCH = "WEB_SEARCH"           # 웹 검색
    FILE_OPERATIONS = "FILE_OPERATIONS" # 파일 읽기/쓰기
    DATABASE = "DATABASE"               # DB 쿼리/수정
    API_CLIENT = "API_CLIENT"           # REST/GraphQL 호출
    NLP_PROCESSING = "NLP_PROCESSING"   # 텍스트 처리
    CODE_EXECUTION = "CODE_EXECUTION"   # 코드 실행
    ML_ANALYTICS = "ML_ANALYTICS"       # 머신러닝 분석
    COMMUNICATION = "COMMUNICATION"     # 알림/메시지
```

#### ToolCategory (6개)

```python
class ToolCategory(str, Enum):
    SEARCH_RETRIEVAL = "SEARCH_RETRIEVAL"
    DATA_PROCESSING = "DATA_PROCESSING"
    SYSTEM_INTEGRATION = "SYSTEM_INTEGRATION"
    CONTENT_GENERATION = "CONTENT_GENERATION"
    MONITORING_ANALYSIS = "MONITORING_ANALYSIS"
    USER_INTERACTION = "USER_INTERACTION"
```

#### ConceptualTool — 개념적 도구 정의

```python
@dataclass
class ConceptualTool:
    name: str                            # 개념적 이름 (예: "web_search_tool")
    tool_type: ToolType
    category: ToolCategory
    description: str
    implementations: List[ToolImplementation]  # 구체적 구현체 목록

@dataclass
class ToolImplementation:
    class_name: str                      # 예: "SerperDevTool"
    package: str                         # 예: "crewai_tools"
    import_statement: str
    requires_api_key: bool
    api_key_env: Optional[str]           # 예: "SERPER_API_KEY"
    config_params: Dict[str, Any]
```

#### tools.json 구조 예시

```json
{
  "web_search_tool": {
    "type": "WEB_SEARCH",
    "category": "SEARCH_RETRIEVAL",
    "description": "인터넷 검색 및 정보 수집",
    "implementations": [
      {
        "class_name": "SerperDevTool",
        "package": "crewai_tools",
        "import": "from crewai_tools import SerperDevTool",
        "requires_api_key": true,
        "api_key_env": "SERPER_API_KEY"
      },
      {
        "class_name": "TavilySearchTool",
        "package": "langchain_community",
        "import": "from langchain_community.tools import TavilySearchTool",
        "requires_api_key": true,
        "api_key_env": "TAVILY_API_KEY"
      }
    ]
  }
}
```

**한국어 도구명 번역 (40+ 쌍)**:

```python
# caas_framework/codegen/tool_generator.py
KOREAN_TO_ENGLISH_TOOLS = {
    "웹 검색 도구": "web_search_tool",
    "파일 읽기 도구": "file_read_tool",
    "데이터베이스 도구": "database_tool",
    "API 호출 도구": "api_client_tool",
    "텍스트 분석 도구": "nlp_processing_tool",
    # ... 40+ 쌍
}
```

### 4.2 에이전트 역할 온톨로지

**위치**: `caas_framework/knowledge/ontology/manager.py`

#### AgentRole 열거형 (12개)

```python
class AgentRole(str, Enum):
    DATA_ANALYST = "data_analyst"
    CONTENT_WRITER = "content_writer"
    RESEARCHER = "researcher"
    API_INTEGRATOR = "api_integrator"
    TASK_COORDINATOR = "task_coordinator"
    QUALITY_ASSURANCE = "quality_assurance"
    SECURITY_SPECIALIST = "security_specialist"
    CUSTOMER_SERVICE = "customer_service"
    DATA_ENGINEER = "data_engineer"
    BUSINESS_ANALYST = "business_analyst"
    MACHINE_LEARNING_ENGINEER = "ml_engineer"
    DEVOPS_ENGINEER = "devops_engineer"
```

#### TaskType 열거형 (8개)

```python
class TaskType(str, Enum):
    DATA_COLLECTION = "data_collection"
    ANALYSIS = "analysis"
    CONTENT_CREATION = "content_creation"
    REPORTING = "reporting"
    INTEGRATION = "integration"
    VALIDATION = "validation"
    COORDINATION = "coordination"
    MONITORING = "monitoring"
```

#### ROLE_TASK_MAPPINGS — 역할별 허용 작업 유형

```python
ROLE_TASK_MAPPINGS: Dict[AgentRole, List[TaskType]] = {
    AgentRole.DATA_ANALYST: [
        TaskType.ANALYSIS,
        TaskType.REPORTING,
        TaskType.DATA_COLLECTION,
    ],
    AgentRole.CONTENT_WRITER: [
        TaskType.CONTENT_CREATION,
        TaskType.RESEARCH,
    ],
    AgentRole.API_INTEGRATOR: [
        TaskType.INTEGRATION,
        TaskType.DATA_COLLECTION,
    ],
    AgentRole.TASK_COORDINATOR: [
        TaskType.COORDINATION,
        TaskType.MONITORING,
    ],
    # ...
}
```

#### OntologyValidator 검증 로직

`caas_framework/validation/ontology_validator.py`는 다음을 검증합니다:

1. **도구 적합성**: Agent에 할당된 도구가 해당 역할의 `ToolType`에 맞는지 확인
2. **작업 유형 일치**: Agent의 `TaskType`이 `ROLE_TASK_MAPPINGS`에 정의된 범위 내인지 확인
3. **의존성 순환**: Task 간 순환 의존성(cycle) 탐지
4. **도구 중복**: 동일 도구가 여러 Agent에 중복 할당되는 비효율 탐지

```python
class OntologyValidator:
    def validate(self, agents: List[AgentSpec], tasks: List[TaskSpec]) -> ValidationResult:
        issues = []
        issues += self._check_tool_compatibility(agents)
        issues += self._check_task_type_mapping(agents, tasks)
        issues += self._check_dependency_cycles(tasks)
        issues += self._check_tool_duplication(agents)
        return ValidationResult(issues=issues)
```

---

## 5. 템플릿 시스템

**위치**: `data/templates/` (26개 Jinja2 `.j2` 파일)

### 5.1 전체 템플릿 목록

```
data/templates/
├── agents.py.j2            # CrewAI Agent 정의
├── tasks.py.j2             # CrewAI Task 정의
├── crew.py.j2              # Crew 조합 + 실행
├── main.py.j2              # 메인 진입점 (Rich CLI)
├── tools.py.j2             # 커스텀 도구 (BaseTool 상속)
├── config.py.j2            # 설정 관리
├── requirements.txt.j2     # 의존성 목록
├── .env.example.j2         # 환경변수 템플릿
├── README.md.j2            # 프로젝트 README
├── backend/
│   ├── models.j2           # Pydantic / SQLAlchemy 모델
│   ├── api.j2              # FastAPI 라우터 + CRUD
│   ├── database.j2         # DB 연결 설정
│   └── schemas.j2          # Pydantic 요청/응답 스키마
├── artifacts/
│   ├── project_proposal.j2     # 프로젝트 기획서
│   ├── requirements_spec.j2    # 요구사항 명세서
│   ├── architecture_design.j2  # 아키텍처 설계서
│   ├── agent_design.j2         # 에이전트 설계서
│   ├── task_design.j2          # 태스크 설계서
│   ├── test_plan.j2            # 테스트 계획서
│   ├── api_spec.j2             # API 명세서
│   ├── deployment_guide.j2     # 배포 가이드
│   ├── code_review.j2          # 코드 리뷰 체크리스트
│   └── project_summary.j2      # 프로젝트 요약서
└── tests/
    ├── test_agents.j2      # Agent 단위 테스트
    ├── test_tasks.j2       # Task 단위 테스트
    └── test_integration.j2 # 통합 테스트
```

### 5.2 주요 템플릿 변수

#### agents.py.j2

```jinja2
{# 입력 변수 #}
{# agents: List[AgentSpec] #}
{# llm_config: Dict (provider, model, temperature) #}
{# tools_import: str (생성된 import 문) #}

from crewai import Agent
{{ tools_import }}

{% for agent in agents %}
{{ agent.id }} = Agent(
    role="{{ agent.role }}",
    goal="{{ agent.goal }}",
    backstory="{{ agent.backstory }}",
    tools=[{% for t in agent.tools %}{{ t }}(), {% endfor %}],
    llm={{ llm_config.provider }},
    verbose=True,
    allow_delegation={{ agent.allow_delegation | lower }},
)
{% endfor %}
```

#### tasks.py.j2

```jinja2
{# 입력 변수 #}
{# tasks: List[TaskSpec] #}
{# agent_refs: Dict[str, str] (task_id → agent_variable_name) #}

from crewai import Task

{% for task in tasks %}
{{ task.id }} = Task(
    description="{{ task.description }}",
    expected_output="{{ task.expected_output }}",
    agent={{ agent_refs[task.agent_id] }},
    {% if task.context %}
    context=[{% for c in task.context %}{{ c }}, {% endfor %}],
    {% endif %}
)
{% endfor %}
```

#### crew.py.j2

```jinja2
{# 입력 변수 #}
{# agents: List[str] (변수명) #}
{# tasks: List[str] (변수명) #}
{# process_type: "sequential" | "hierarchical" #}
{# manager_llm: Optional[str] (hierarchical 시 필수) #}

from crewai import Crew, Process

crew = Crew(
    agents=[{{ agents | join(', ') }}],
    tasks=[{{ tasks | join(', ') }}],
    process=Process.{{ process_type | upper }},
    {% if process_type == "hierarchical" %}
    manager_llm={{ manager_llm }},
    {% endif %}
    verbose=True,
)
```

### 5.3 템플릿 렌더링 엔진

```python
# caas_framework/codegen/engine.py

from jinja2 import Environment, FileSystemLoader

class CodeGenEngine:
    def __init__(self, template_dir: Path):
        self.env = Environment(
            loader=FileSystemLoader(str(template_dir)),
            trim_blocks=True,
            lstrip_blocks=True,
        )

    def render(self, template_name: str, context: Dict[str, Any]) -> str:
        template = self.env.get_template(template_name)
        return template.render(**context)

    def generate_all(
        self,
        golden_data: ConcretizedRequirement,
        agents: List[AgentSpec],
        tasks: List[TaskSpec],
        strategy: DomainStrategyConfig,
        output_dir: Path,
    ) -> Dict[str, str]:
        """도메인 전략에 따라 필요한 파일을 선택적으로 생성"""
        files = {}

        # 모든 도메인 공통
        files["agents.py"] = self.render("agents.py.j2", {...})
        files["tasks.py"] = self.render("tasks.py.j2", {...})
        files["crew.py"] = self.render("crew.py.j2", {...})
        files["main.py"] = self.render("main.py.j2", {...})

        # CRUD_BASED / HYBRID
        if strategy.enable_crud:
            files["backend/models.py"] = self.render("backend/models.j2", {...})
            files["backend/api.py"] = self.render("backend/api.j2", {...})

        # UI 명시적 요청 시만
        if strategy.enable_frontend:
            files["app.py"] = self.render("app.j2", {...})  # Streamlit

        return files
```

---

## 6. ValidationOrchestrator — 7개 검증기

**위치**: `caas_framework/validation/orchestrator.py`

```python
class ValidationOrchestrator:
    validators = [
        CodeQualityValidator,    # 코드 품질 (복잡도, 스타일)
        CrewAIValidator,         # CrewAI API 호환성 검증
        DependencyValidator,     # Task 의존성 DAG 검증
        GoldenDataValidator,     # Golden Data 완전성 검증
        OntologyValidator,       # 온톨로지 일치성 검증 ← 위에서 설명
        Python311Validator,      # Python 3.11+ 호환성
        TaskValidator,           # Task 설계 완전성
    ]
```

각 Validator의 출력은 `ValidationResult(issues=List[ValidationIssue])`이며, `AutoFixer`가 이를 소비하여 3-Level 자동 수정을 시도합니다.

---

## 7. 데이터 구조 간 관계 요약

```
ConcretizedRequirement (Golden Data)
    ├── domain: DomainType ──────────────────────────────────────────────┐
    ├── features: List[FeatureSpec]                                       │
    │       └── tools_needed → Tool Ontology (도구 추천)                  │
    ├── data_models: List[DataModel]                                      │
    │       └── → backend/models.j2 (CRUD 전략 시)                        │
    └── ui_components: List[UIComponent]                                  │
            └── → app.j2 (명시적 UI 요청 시)                               │
                                                                          │
DomainType ──────────────────── DomainStrategyConfig ──────────────────►┘
    │                               │
    │                               ├── CodeGenStrategy
    │                               │     AGENT_BASED → agents.py.j2, tasks.py.j2
    │                               │     CRUD_BASED  → backend/*.j2
    │                               │     HYBRID      → 양쪽 모두
    │                               │
    │                               └── recommended_tools → Tool Ontology
    │                                                            │
domain_keywords.json                                    tools.json (50+)
    └── 226개 키워드                                   ConceptualTool
        → 자동 분류                                    ToolImplementation

AgentRole × TaskType
    ↓ ROLE_TASK_MAPPINGS
    → OntologyValidator 검증
    → agents.py.j2 렌더링 변수
```

---

## 8. 커스터마이징 가이드

### 새로운 도메인 추가

1. `caas_framework/models/domain_types.py`에 `DomainType` 열거값 추가
2. `data/ontology/domain_keywords.json`에 키워드 목록 추가
3. `caas_framework/codegen/domain_strategy.py`에 `DomainStrategyConfig` 추가
4. (필요 시) `data/templates/`에 도메인 특화 템플릿 추가

### 새로운 도구 추가

1. `data/ontology/tools.json`에 새 도구 항목 추가
2. (선택) `caas_framework/knowledge/ontology/tool_ontology.py`에 `ToolType` 확장
3. 한국어 도구명이 있으면 `caas_framework/codegen/tool_generator.py`의 번역 맵 추가

### 새로운 에이전트 역할 추가

1. `caas_framework/knowledge/ontology/manager.py`에 `AgentRole` 열거값 추가
2. `ROLE_TASK_MAPPINGS`에 허용 `TaskType` 목록 추가
3. `OntologyValidator` 검증 규칙에 반영

---

## 관련 문서

- [60_도메인_레퍼런스.md](60_도메인_레퍼런스.md) — 도메인별 상세 사양
- [61_API_레퍼런스.md](61_API_레퍼런스.md) — Python SDK API
- [62_용어집.md](62_용어집.md) — 핵심 용어 정의
- [../2_개발_실무_가이드/13_Agent_Task_설계_가이드.md](../2_개발_실무_가이드/13_Agent_Task_설계_가이드.md) — 에이전트 설계 실무
- [../2_개발_실무_가이드/12_Golden_Data_활용법.md](../2_개발_실무_가이드/12_Golden_Data_활용법.md) — Golden Data 작성법

---

**Last Updated**: 2026-03-13 (v0.6.4)
